spring:
  application:
    name: config-server
  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BeKhaled/micro-service-configure
    
security:
  user:
    password: password
    
server:
  port: 8888 